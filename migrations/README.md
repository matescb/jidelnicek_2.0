# Database Migrations Directory

This directory contains all database migration files for schema management.

## Purpose

- Track database schema changes over time
- Enable version control for database structure
- Support rollback capabilities
- Ensure consistent database state across environments

## Naming Convention

Migration files should follow a consistent naming pattern:
`YYYYMMDD_HHMMSS_description_of_change.sql` or framework-specific conventions.